import React from 'react'
import "./Header.scss"

const Header = (props) => {
  return (
    <div className="heroImage" style={{ height: props.height + "vh" }}>

    </div>
  )
}

export default Header
