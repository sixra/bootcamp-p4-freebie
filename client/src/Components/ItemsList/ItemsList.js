import React from "react";
import "./ItemsList.scss";

const ItemsList = ({ places }) => {
  return (
    <div>
      <h1>ITEMS LIST</h1>

      {/* {places.map((place) => (
        <p>
          This is {place.name} from {place.country}
        </p>
      ))} */}
    </div>
  );
};

export default ItemsList;
